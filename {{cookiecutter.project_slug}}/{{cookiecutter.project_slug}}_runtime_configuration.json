{
  "common_variable": {
    "connection_port": 22,
    "default_prompt": ">\\s*$",
    "enable_prompt": "#\\s*$",
    "config_prompt": "[(]config.*[)]#\\s*$"
  },
  "driver_variable": {
    "resource_name": [
      "Unused",
      "Blade {address}",
      "Port {address}"
    ],
    "resource_family_name": [
      "{{cookiecutter.family_name}}",
      "{{cookiecutter.family_name}} Blade",
      "{{cookiecutter.family_name}} Port"
    ],
    "resource_model_name": [
      "{{cookiecutter.model_name}}",
      "Blade {{cookiecutter.model_name}}",
      "Port {{cookiecutter.model_name}}"
    ]
  }
}
